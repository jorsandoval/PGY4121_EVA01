import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PGY4121_EVA01',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
